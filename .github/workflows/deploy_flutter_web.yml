name: Disabled - No GitHub Pages Deploy

on:
  workflow_dispatch:

jobs:
  disabled:
    name: GitHub Pages deployment disabled
    runs-on: ubuntu-latest

    steps:
      - name: Skip deployment
        run: echo "GitHub Pages deployment is disabled for this repository."
